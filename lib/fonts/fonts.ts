import { Inter, Oswald, Knewave } from 'next/font/google';

export const inter = Inter({
	subsets: ['latin'],
});

export const oswald = Oswald({
	subsets: ['latin'],
});

export const kneawe = Knewave({
	weight: '400',
	subsets: ['latin'],
	display: 'swap',
});
